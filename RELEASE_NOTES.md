## [v2.4.0] - 2026-06-08

### Added
- **页面美化功能**：新增管理后台「页面美化」配置页，支持管理员自定义主题色彩（主色调、页面背景色、卡片背景色、文字颜色、边框颜色）、布局调整（卡片圆角、阴影、内边距、间距）、排版设置（标题字号、字重、描述字号）以及自定义 CSS 注入
- **渐变毛玻璃预设主题**：新增第 7 个预设主题「渐变毛玻璃」，包含渐变背景、毛玻璃卡片、色彩消隐盾等完整视觉方案
- **备案栏条件隐藏**：当管理后台未配置工信部备案信息时，底部备案栏自动隐藏，减少页面空白占用
- **CSS 变量主题系统**：前端全局采用 CSS 变量（`--van-nav-*`）实现主题定制，零运行时性能损耗
- **主题备份集成**：主题配置无缝接入现有导入导出与 WebDAV 备份系统

### Fixed
- **中文输入法首字符变英文**：搜索框全局按键监听在输入法合成期间（`ev.isComposing` / `keyCode === 229`）不再触发 `focus()`，彻底修复中文输入法被打断的问题
- **搜索时页面跳动**：禁用浏览器滚动锚定（`overflow-anchor: none`），搜索结果数量变化时页面不再上下跳动
- **sticky 搜索栏跳回原位**：搜索时不再切换分类标签状态，避免组件重渲染导致 sticky 搜索栏丢失吸附位置
- **搜索结果被 sticky 栏遮挡**：搜索触发时自动平滑滚动至顶部，确保结果完整可见
- **页面无法滚动**：解除 `body` 和 `.main` 的 `overflow: hidden`，恢复页面自然流式滚动
- **主题配置不生效**：修复前端组件未使用 CSS 变量导致主题色彩、布局、排版配置无法应用到实际页面的问题
- **CSS 净化正则不兼容**：修复自定义 CSS 净化正则表达式不兼容 Go RE2 语法导致服务启动 panic 的问题

### Changed
- **主页面布局重构**：从固定视口（`96vh + overflow: hidden`）重构为三段式自然流布局——搜索栏 `position: sticky` 吸附顶部、卡片区域自然滚动、备案栏 `position: fixed` 固定底部
- **滚动懒加载适配**：滚动监听从容器级（`.content-wraper`）迁移至窗口级（`window`），适配新的自然流布局
- **搜索框 autoFocus 优化**：桌面端自动聚焦搜索框，移动端（<700px）不聚焦以避免弹出软键盘
- **标签选择器间距自适应**：移除标签选择器固定的 `margin-bottom`，间距随标签行数动态变化
- **代码审计优化**：`overflow-anchor` 选择器收窄、`scrollTo` 使用 `rAF` + `smooth`、长 CSS 提取为模块常量、清理未使用的 `contentRef`

### 风险提示
本项目代码包含部分由 AI 自动生成与修改的逻辑。为确保数据资产安全，请在升级前务必对底层物理数据库（data/nav.db）进行完整备份，并自行评估导入风险。

---

### 升级注意事项
1. **数据库变更**：新增 `nav_theme_config` 表（单行配置，`CHECK (id = 1)` 约束），不影响现有数据
2. **前端布局变更**：主页面从固定高度布局改为自然流式布局，如有自定义 CSS 可能需要适配
3. **自定义 CSS 安全**：后端对自定义 CSS 执行 10KB 截断 + 危险关键字正则净化（`expression`、`behavior`、跨域 `url()` 等）
4. **Go 版本要求**：Go 1.24+（Dockerfile 已在 v2.3.2 中升级）
