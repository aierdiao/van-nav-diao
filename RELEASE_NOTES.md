## [v2.4.1] - 2026-06-09

### Fixed
- **中文输入法首字符变英文**：彻底修复全局按键聚焦搜索框时打断中文输入法合成的问题。采用「常驻聚焦 + 智能唤回」方案——页面加载后搜索框保持聚焦，点击空白区域自动抓回焦点，点击链接/按钮等可聚焦元素时正常放行。移除了导致问题的全局 `document.addEventListener("keydown")` 监听器
- **暗色模式不生效**：暗色模式下仅卡片分类变色，页面背景、文字、边框等未切换。根因是 CSS 变量（`--van-nav-bg-base` 等）缺少 `body.dark-mode` 覆盖，现已添加完整的暗色模式变量定义
- **页面返回后搜索框失焦**：点击卡片跳转外部页面后返回，搜索框失去焦点导致无法直接输入。新增 `visibilitychange` 事件监听，页面重新可见时自动恢复焦点

### 风险提示
本项目代码包含部分由 AI 自动生成与修改的逻辑。为确保数据资产安全，请在升级前务必对底层物理数据库（data/nav.db）进行完整备份，并自行评估导入风险。

---

### 升级注意事项
1. 本次为纯前端 Bug 修复，无数据库变更、无 API 变更
2. 搜索框交互方式变更：移除全局按键监听器，改为常驻聚焦模式。用户无需点击搜索框即可直接输入，点击空白区域焦点自动恢复
3. 暗色模式 CSS 变量覆盖仅影响 `body.dark-mode` 下的样式，不影响亮色模式
