## [v2.4.2] - 2026-06-12

### Added
- **在线 Demo 体验**：新增演示站点链接，用户可在无需部署的情况下快速体验 VanNav 功能

### Fixed
- **Token 安全加固**：修复密码修改后旧 Token 仍可使用的问题，提升系统安全性

### Changed
- **API Token 过期时间优化**：从 10 年缩短至 1 年，降低 token 泄露后的攻击窗口
- **Docker 部署优化**：新增 docker-entrypoint.sh 脚本，简化容器启动配置
- **Dockerfile 完善**：优化镜像构建逻辑，提升部署体验

---

## 升级注意事项

1. **数据库兼容性**：本次更新新增 `nav_user.token_version` 字段，现有数据库会自动迁移，无需手动操作
2. **Token 影响**：
   - 普通登录 Token：不受影响，会自动携带新版本号
   - API Token：不受影响，走独立验证通道
   - 修改密码后，旧 Token 会立即失效（这是预期行为）

## 风险提示

本项目代码包含部分由 AI 自动生成与修改的逻辑。为确保数据资产安全，请在升级前务必对底层物理数据库（data/nav.db）进行完整备份，并自行评估导入风险。
