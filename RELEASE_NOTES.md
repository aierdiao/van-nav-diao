## [v2.2.0] - 2026-06-06

### Added

- **主页面管理后台浮窗按钮**：在主页面右下角（GitHub 图标和夜间模式切换上方）新增齿轮图标按钮，点击可快速进入管理后台。按钮样式与现有图标风格统一，支持暗色模式，点击行为跟随用户的跳转设置（当前页跳转或新窗口打开），受管理员"隐藏管理后台卡片"设置控制

### Fixed

- **GoReleaser v2 Release Notes 注入失效**：GoReleaser v2 的 `--release-notes` CLI flag 静默无效，导致 GitHub Release 页面 Body 为空。改为构建完成后通过 `gh release edit --notes-file` 后置注入

### 风险提示

本项目代码包含部分由 AI 自动生成与修改的逻辑。为确保数据资产安全，请在升级前务必对底层物理数据库（data/nav.db）进行完整备份，并自行评估导入风险。

---

## 升级注意事项

1. 本次更新无数据库 schema 变更，可直接替换二进制升级
2. 管理后台浮窗按钮默认显示，管理员可在后台"设置 → 站点设置 → 隐藏管理员后台卡片"中关闭
